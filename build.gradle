buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.1.1'
    }
}
// تم حذف allprojects لأن المستودعات معرفة في settings.gradle
task clean(type: Delete) {
    delete rootProject.buildDir
}